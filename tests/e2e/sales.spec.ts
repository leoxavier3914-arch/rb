import { test } from "@playwright/test";

test.describe.skip("Vendas smoke", () => {
  test("scroll infinito e exportação", async () => {
    // Requer ambiente com dados reais e servidor em execução.
  });
});
